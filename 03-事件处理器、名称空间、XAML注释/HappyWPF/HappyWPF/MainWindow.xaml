<Window x:Class="HappyWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HappyWPF"
        xmlns:control="clr-namespace:ControlLibrary;assembly=ControlLibrary"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="340"
        Width="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <control:SalaryCalculator Grid.Row="0"
                                  Grid.Column="0" />
        <control:SalaryCalculator Grid.Row="0"
                                  Grid.Column="1" />
        <control:SalaryCalculator Grid.Row="1"
                                  Grid.Column="0" />
        <control:SalaryCalculator Grid.Row="1"
                                  Grid.Column="1" />
    </Grid>
</Window>
